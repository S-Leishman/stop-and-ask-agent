# STRANDS-SPIKE-001 Dependency Evidence

Observed in the WSL test environment on 2026-09-04:

```text
strands-agents==1.54.0
cryptography==50.0.1
pytest==9.1.1
```

Install the declared direct dependencies with:

```text
python -m pip install -r requirements.txt
```

This records observed versions for the submission evidence. It is not a
reproducible full transitive lockfile.
